<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="ui.money_transfers.search.MoneyTransferSearchFragment">

    <data>
        <variable name="visibility" type="Integer" />
        <variable name="transferNumber" type="String" />
        <variable name="transferDate" type="String" />
    </data>

    <LinearLayout
        android:id="@+id/root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/topPanel"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/goBack"
                style="@style/Widget.AppCompat.ActionButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/go_back"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                android:src="@drawable/ic_back" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/search"
                android:textAlignment="center"
                android:textSize="20sp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintTop_toTopOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>

        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:indeterminate="true" />

        <EditText
            android:id="@+id/search"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:drawableEnd="@drawable/ic_search_red"
            android:paddingStart="@dimen/sixteen_dp"
            android:paddingEnd="@dimen/sixteen_dp"
            android:inputType="textFilter"
            android:imeOptions="actionSearch"
            android:maxLines="1"
            android:layout_margin="@dimen/sixteen_dp" />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/transfer_item"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/my_items_list_item_shape"
            android:visibility='@{visibility}'
            android:padding="@dimen/sixteen_dp"
            android:layout_margin="@dimen/sixteen_dp"
            android:layout_marginTop="@dimen/twenty_four_dp">

            <TextView
                android:id="@+id/transfer_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text='@{transferNumber}'
                android:textSize="15sp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toStartOf="@id/transfer_status_indicator"
                tools:text="MX342423"/>

            <TextView
                android:id="@+id/transfer_date_view"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text='@{transferDate}'
                android:textSize="12sp"
                android:layout_marginTop="@dimen/four_dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/transfer_name"
                app:layout_constraintEnd_toStartOf="@id/transfer_status_indicator"
                tools:text="12.03.1231" />

            <ImageView
                android:id="@+id/transfer_status_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="@id/transfer_name"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="@id/transfer_date_view"
                tools:src="@drawable/ic_received"/>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </LinearLayout>
</layout>